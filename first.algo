ALGORITHM palindrome
VAR
    first : CHAR
    second : CHAR
    i : 
BEGIN
    read(i)
    read(j)
     i = first.lenght-1
     j = second.lenght-1
    while (i > j) 
        if (word[i] := word[j]) {
            return false;
            BREAK ;
        }
        while (i=j) {
        if (word[i] := word [j])
    }
    return true;
END